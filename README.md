# AVR project template for VS Code + Dev Containers

Uses `avr-g++` to compile files.

Installs `C/C++ Extension Pack` and `Header Guard Generator` inside container.

## How to:
### Build the firmware
```
make
```

### Clean the build directory
```
make clean
```